script, mainloop, begin
	while (true) do (
		wait()
		update()
		game:tick += 1
	)
	subscript, update, begin
		switch (game:state) do (
			case (state:title) do (
				updateTitle
				#animate()
			)
		)
		
	end
end

script, animate, begin
end